name: flutter_application_1
description: "Elección Delegado 2026 - Ingeniería de Sistemas e Informática"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.4.2
  supabase_flutter: ^2.1.0
  flutter_dotenv: ^5.2.1
  envied: ^0.5.0
  provider: ^6.1.1
  cupertino_icons: ^1.0.2
  geolocator: ^11.0.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - .env
    - assets/images/candidates/
